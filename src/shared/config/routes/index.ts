export const ROUTES = {
  home: '/',
  rules: '/rules',
  schedule: '/schedule',
  races: '/races',
  replays: '/replays',
  auth: {
    signin: '/auth/signin',
    signup: '/auth/signup',
  },
} as const;
